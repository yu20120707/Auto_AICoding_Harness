# Implementation Plan

1. Capture the requested task in `.ai/spec.md`.
   verify: task scope is specific to the sample server and not generic filler.
2. Describe module boundaries and risk in `.ai/tech-design.md`.
   verify: business files and harness files are clearly separated.
3. Mirror the same task under `docs/ai/tasks/sample-server-hardening/`.
   verify: `00` through `07` files align with `.ai/state.json::task_id`.
4. Keep verification honest.
   verify: evidence distinguishes "planned" from "already run".
