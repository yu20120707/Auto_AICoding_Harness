# Handoff

Current state:

- repository is in `large` mode
- task id is `sample-server-hardening`
- planning artifacts exist
- verification and review gates have not been executed yet

Next step:

- run `ai-review spec` to start the real large-mode gate chain
